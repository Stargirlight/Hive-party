# Hive Party - PHP Application
RewriteEngine On

# Route all API requests to index.php
RewriteRule ^api/(.*)$ api/index.php [L,QSA]

# Allow direct access to existing files
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]
